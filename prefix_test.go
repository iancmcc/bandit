package bandit_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
	//. "github.com/iancmcc/bandit"
)

var _ = Describe("Prefix", func() {
	It("should merge prefixes", func() {
		Ω(true).Should(BeTrue())
	})

})
